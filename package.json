{
  "name": "cyh-browser-skill",
  "version": "1.4.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "check": "node --check scripts/runtime-core.mjs && node --check scripts/semantic-snapshot.mjs && node --check scripts/cdp-proxy.mjs && node --check scripts/ext-bridge.mjs && node --check extension/background.js"
  },
  "engines": {
    "node": ">=22"
  }
}
